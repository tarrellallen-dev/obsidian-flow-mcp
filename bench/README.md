# bench

Placeholder. Empty by design at build step 1.

This directory will hold, at build step 5:

- a seeded synthetic tick generator that drives the same rings and calculators as the live path;
- a frame replayer that feeds a recorded frame log into the server side for CI;
- the report renderer that emits the tables described in `docs/latency-methodology.md`.

Nothing here yet. No number exists to render.
